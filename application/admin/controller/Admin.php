<?php
  namespace app\admin\controller;

  // use think\view;
  // use think\controller;

  class Admin
  {
      public function lst()
      {
        return view();

        // $view = new \think\view([
        //     'view_suffix'  => 'htm',
        // ]);
        // return $view -> fetch();

        // $view = new view([
        //     'view_suffix'  => 'htm',
        // ]);
        // return $view -> fetch();

        // return $this -> fetch();
      }
      public function add()
      {
        return view();
      }
      public function edit()
      {
        return view();
      }
  }
